# Claude Code Statusline

Кастомный statusline для [Claude Code](https://docs.anthropic.com/en/docs/claude-code) с расширенной информацией.

![preview](preview.png)

## Что показывает

| Иконка | Описание |
|--------|----------|
| 📁 | Текущая директория |
| 🔀 | Git ветка |
| ⏱️ | Время с последнего коммита |
| 🤖 | Модель (сокращённо: O4.5, S4.5, H4) |
| 💰 | Стоимость: сессия • сегодня / всего |
| 📊 | Прогресс-бар использования контекста |

## Требования

- [Nerd Font](https://www.nerdfonts.com/) для иконок
- `jq` для парсинга JSON
- `bc` для вычислений
- [bun](https://bun.sh/) + [ccusage](https://github.com/ryoppippi/ccusage) для статистики расходов (опционально)

## Установка

### 1. Установите зависимости

```bash
brew install jq bc
```

### 2. (Опционально) Установите bun и ccusage для статистики расходов

```bash
curl -fsSL https://bun.sh/install | bash
```

### 3. Скопируйте скрипт

```bash
mkdir -p ~/.claude
curl -o ~/.claude/token-counter.sh https://raw.githubusercontent.com/fsedis/claude-statusline/master/token-counter.sh
chmod +x ~/.claude/token-counter.sh
```

### 4. Добавьте конфигурацию в `~/.claude/settings.json`

```json
{
  "statusLine": {
    "type": "command",
    "command": "bash ~/.claude/token-counter.sh"
  }
}
```

Или выполните команду:

```bash
# Если файл уже существует
jq '. + {"statusLine": {"type": "command", "command": "bash ~/.claude/token-counter.sh"}}' ~/.claude/settings.json > tmp.json && mv tmp.json ~/.claude/settings.json

# Если файла нет
echo '{"statusLine": {"type": "command", "command": "bash ~/.claude/token-counter.sh"}}' > ~/.claude/settings.json
```

### 5. Перезапустите Claude Code

```bash
claude
```

## Цветовая индикация

Прогресс-бар контекста меняет цвет в зависимости от заполненности:

| Цвет | Значение |
|------|----------|
| 🟢 Зелёный | < 50% |
| 🟡 Жёлтый | 50-80% |
| 🔴 Красный | > 80% |

## Кастомизация

Скрипт написан на bash и легко модифицируется. Основные переменные:

```bash
# Иконки (Nerd Font)
ICON_FOLDER="󰉋"
ICON_GIT="󰊢"
ICON_MODEL="󰧑"
# ...

# Цвета (ANSI)
BLUE='\033[34m'
GREEN='\033[32m'
# ...
```

## Лицензия

MIT
